import { action } from "@storybook/addon-actions";
import { Layout } from "svo";

export default {
  title: "Layout/Layout",
  component: Layout,
};

export const Default = () => ({
  Component: Layout,
  props: {},
});
